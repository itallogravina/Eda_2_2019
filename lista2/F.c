#include <stdio.h>

int main(int argc, char const *argv[]) {
  char str[10001],str1[41];
  int i=0;
  scanf("%[^\n]s",str);
  scanf("%s",str1 );

while (str[i]!='\0') {
  i++;
}
  for (int j = 0; j < i; j++) {
    for (int p = 0; p < ; p++) {
    if()
    }
  }
  return 0;
}
